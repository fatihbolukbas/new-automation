
{
    "default": {
      "paths": ["tests/feature/**/*.feature"],
      "formatOptions": {
        "snippetInterface": "async-await"
      },
      "require": [
        "tests/steps/**/*.ts",
        "hooks/**/*.ts"
      ],
      "requireModule": ["ts-node/register"]
}
}
